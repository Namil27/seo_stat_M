# Используем официальный образ Certbot
FROM certbot/certbot:v2.6.0

# Указываем команду по умолчанию, чтобы контейнер не завершался сразу
RUN tail -f /dev/null
# Копируем скрипт для настройки сертификатов
# (Если у вас есть специальный скрипт, можно скопировать его сюда)
# COPY script.sh /app/script.sh

# Делаем скрипт исполняемым (если используется)
# RUN chmod +x /app/script.sh

# Команда по умолчанию для контейнера
# В данном случае мы не указываем команду по умолчанию,
# потому что мы будем запускать её вручную.
